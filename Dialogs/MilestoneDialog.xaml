<Window x:Class="wada.Dialogs.MilestoneDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Milestone" Height="200" Width="320"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Content="Milestone Name *" />
        <TextBox Grid.Row="1" x:Name="TxtDescription" Margin="0,0,0,6" />

        <Label Grid.Row="2" Content="Deadline" />
        <DatePicker Grid.Row="3" x:Name="DpDeadline" Margin="0,0,0,6" />

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK"     Width="72" Margin="0,0,8,0" Click="OkButton_Click" IsDefault="True" />
            <Button Content="Cancel" Width="72"                   Click="CancelButton_Click" IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>
