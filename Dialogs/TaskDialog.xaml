<metro:MetroWindow x:Class="wada.Dialogs.TaskDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:metro="http://metro.mahapps.com/winfx/xaml/controls"
        Title="Add Task" Height="320" Width="360"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        GlowBrush="{DynamicResource MahApps.Brushes.Accent}"
        BorderThickness="1"
        ShowMinButton="False" ShowMaxRestoreButton="False">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Content="Task Name *" />
        <TextBox Grid.Row="1" x:Name="TxtName"
                 metro:TextBoxHelper.Watermark="Enter task name…"
                 metro:TextBoxHelper.ClearTextButton="True"
                 Margin="0,0,0,10" />

        <Label Grid.Row="2" Content="Description (optional)" />
        <TextBox Grid.Row="3" x:Name="TxtDescription"
                 metro:TextBoxHelper.Watermark="Enter description…"
                 Height="56" TextWrapping="Wrap" AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"
                 Margin="0,0,0,10" />

        <Label Grid.Row="4" Content="Deadline" />
        <DatePicker Grid.Row="5" x:Name="DpDeadline" Margin="0,0,0,10" />

        <StackPanel Grid.Row="7" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" Width="80" Margin="0,0,8,0"
                    Style="{DynamicResource MahApps.Styles.Button.Square.Accent}"
                    Click="OkButton_Click" IsDefault="True" />
            <Button Content="Cancel" Width="80"
                    Style="{DynamicResource MahApps.Styles.Button.Square}"
                    Click="CancelButton_Click" IsCancel="True" />
        </StackPanel>
    </Grid>
</metro:MetroWindow>