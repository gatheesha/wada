<UserControl x:Class="wada.Views.ClientsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:wada.Views"
        mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Clients Directory (Contact Book)" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>

        <DockPanel Grid.Row="1" Margin="0,0,0,10">
            <Label Content="Search/Filter:" VerticalAlignment="Center"/>
            <TextBox x:Name="TxtSearchClient" Margin="5,0,0,0" Padding="3"/>
        </DockPanel>

        <ListView Grid.Row="2" x:Name="LvwClients">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}" Width="150"/>
                    <GridViewColumn Header="Mobile Number" DisplayMemberBinding="{Binding MobileNumber}" Width="120"/>
                    <GridViewColumn Header="Email" DisplayMemberBinding="{Binding Email}" Width="180"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
